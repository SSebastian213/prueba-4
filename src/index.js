import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import PostItApp from './App';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <PostItApp />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
